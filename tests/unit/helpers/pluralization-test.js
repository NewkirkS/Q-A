import { pluralization } from 'message-board/helpers/pluralization';
import { module, test } from 'qunit';

module('Unit | Helper | pluralization');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = pluralization([42]);
  assert.ok(result);
});
